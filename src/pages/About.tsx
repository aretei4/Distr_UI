import React from 'react';

const About: React.FC = () => (
  <div>
    <h1 className="text-2xl font-bold mb-4">About</h1>
    <p>This is a production-ready React + TS app deployed via Spring Boot.</p>
  </div>
);

export default About;
